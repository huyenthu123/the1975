let itemsCart =JSON.parse(localStorage.getItem('Cart'));

// thực hiện chạy vòng chạy lại hiển thị lên danh sách sp trong cart
// thực hiện tính toán, báo giá, tổng tiền

async function f() {
    return THE;
}

f().then(alert);

// await